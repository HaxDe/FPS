using UnityEngine;
using System.Collections;

public class CamReturnPos : MonoBehaviour {
	private int resetBuffer = 1;
	void Update () {
	/*	if (!PlayerControl.isControllingZeppelin)
		{
			if (resetBuffer == 1)
				transform.position = Camera.main.transform.position;

			resetBuffer++;

			if (resetBuffer == 3)
				resetBuffer = 1;
		}*/
	}
}
 